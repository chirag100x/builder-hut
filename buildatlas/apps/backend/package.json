{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "bash -c 'if [ ! -d venv ]; then python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt; else source venv/bin/activate; fi && uvicorn main:app --reload --port 8000'"
  }
}
